(declare-fun f (Int Int) Int)
(declare-fun p (Int Int Int Int Int Int) Bool)
(define-fun formula () Bool
	    (forall ((x Int))
	    	    (forall ((y Int))
		    	    (p (f x y) x x y (f x y) x)
		)
	    )
)
(check-sat-using (then qe smt))
(get-model)