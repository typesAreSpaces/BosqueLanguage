namespace NSMain;

entrypoint function main() : Int {

  var filled_list = List<Int>::fill(10, 1);
  var empty_list = List<Int>@{};
  var i = filled_list.countIf(fn(x) => x == 0);
  var j = empty_list.countIf(fn(x) => x == 0);

  check 0 == i;

  return 0;
}
